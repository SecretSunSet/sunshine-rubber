# Copy to `.env` and fill in. Never commit the real `.env`.
# The Resend key is managed separately through Firebase Secret Manager.

# --- Messenger chat widget (client-exposed; must be prefixed PUBLIC_) ---
# The company's real Messenger link, e.g. https://m.me/sunshinerubberph
PUBLIC_MESSENGER_URL=https://m.me/
